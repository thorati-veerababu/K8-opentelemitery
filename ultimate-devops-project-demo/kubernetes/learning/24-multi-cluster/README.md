# 🌐 Multi-Cluster Kubernetes

Managing multiple clusters (e.g., EKS in different regions).

## 📌 Approaches
*   **KubeFed**: Federation (older).
*   **Service Mesh Federation**: Connecting Istio meshes.
*   **External DNS**: Routing traffic to different clusters.
*   **GitOps**: Applying the same config to multiple clusters via ArgoCD.

## 🛠️ Tips
Use `kubectx` to switch contexts easily.
